[package]
name = "aqua"
version = "0.1.0"
edition = "2021"
description = "AquaFeedFormulator Rust CLI — P0.5 最小工程闭环"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
